% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SL.R
\name{screen.glmnet.extra}
\alias{screen.glmnet.extra}
\title{SuperLearner screener using cv.glmnet that works with the extra families}
\usage{
screen.glmnet.extra(
  Y,
  X,
  family,
  alpha = 1,
  minscreen = 2,
  nfolds = 10,
  nlambda = 100,
  ...
)
}
\arguments{
\item{Y}{see \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}}

\item{X}{see \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}}

\item{family}{similar to \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}. Unlike \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}, the entire family object rather than the name of \code{family} (e.g., "gaussian", "binomial") will be passed to \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}}

\item{alpha}{see \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}}

\item{minscreen}{see \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}}

\item{nfolds}{see \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}}

\item{nlambda}{see \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}}

\item{...}{see \code{\link[SuperLearner:SL.glmnet]{SuperLearner::SL.glmnet()}}}
}
\description{
A wrapper of \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}} similar to \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}}, except that \code{family} can be \code{binomial_extra}. \code{\link[SuperLearner:write.screen.template]{SuperLearner::screen.glmnet()}} only passes the name of \code{family} (e.g., "gaussian", "binomial") and thus cannot pass the full customized families like \code{\link[=binomial_extra]{binomial_extra()}}.
}
\examples{
if (interactive()){
    set.seed(321)
    expit <- binomial()$linkinv
    X <- matrix(rnorm(100 * 5), nrow = 100)
    y <- expit(1 + X[,1]) + rnorm(100)
    require(SuperLearner)
    SL.library<-list(c("SL.glmnet.extra","screen.glmnet.extra"))
    SuperLearner(y, data.frame(X), family=binomial_extra(),
                 SL.library = SL.library, cvControl = list(V = 2))
}
}
